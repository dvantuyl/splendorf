import { IGameConfig } from '../../index';
import bgioGame from '../src/game';
import bgioBoard from '../src/board';

const config: IGameConfig = {
  bgioGame,
  bgioBoard,
  debug: false,
};

export default config;